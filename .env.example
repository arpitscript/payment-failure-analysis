# copy to .env and fill in, or export these in your shell before running load_data.py
PGHOST=localhost
PGPORT=5432
PGDATABASE=payments
PGUSER=postgres
PGPASSWORD=

# alternatively, a single connection string (Neon/Supabase give you one):
# DATABASE_URL=postgresql://user:password@host:5432/payments
